/**
 * SOULATH MANMANYSOK
 */

 let sub = 'Hello World'

 let index = 0, length = 9;
 
 function substr(sub, index, length) {
   
     var substring = sub.substr(index, length);
       
     console.log(substring);
 }
 
 substr(sub, index, length);